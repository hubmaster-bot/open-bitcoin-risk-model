# Install OBRM v0.10.5

1. Create and activate a Python 3.11+ virtual environment.
2. Run `pip install -r requirements.txt`.
3. Run `pytest`.
4. Launch with `streamlit run app.py`.

Provider credentials remain optional and must be supplied through environment variables. No secrets are included in the repository.
